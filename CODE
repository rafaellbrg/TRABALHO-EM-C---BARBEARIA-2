/*
NOME DO SISTEMA v1.0
Grupo: RAFAEL BORGES E NICOLAS MIGUEL
Data: 20/05/2026

DESCRIÇÃO:
UM CLIENTE QUERENDO AGENDAR UM CORTE DE CABELO, RESOLVE DIFICULDADES PARA AGENDAMENTO OU NECESSIDADE DE IDA PRESENCIAL NA BARBEARIA 
PARA AGENDAR UM CORTE


FUNCIONAMENTO:
UM SISTEMA SIMPLES ONDE SE ESCOLHE O CORTE, INFORMA SEUS DADOS, TIPO DE PAGAMENTO, QUANTO IRA PAGAR E RECEBE UM RESUMO NO FINAL COM
SEUS DADOS, QUAL FORMA DE PAGAMENTO ESCOLHEU, QUAL CORTE ESCOLHEU, TROCO SE TIVER ETC

CÓDIGO DA APLICAÇÃO:

#include <stdio.h>
int main(){
	#include <stdio.h>

int main() {

    char nome[50];
    float idade, valorT = 0, valorP, troco = 0, valorSemDesconto;
    int qi, i, pagamento;
    int cortes[10];

    printf("\n------BARBEARIA RAFANICO-----\n");
    printf("\nQuantos cortes voce quer agendar? ");
    scanf("%d", &qi);

    // ESCOLHA DOS CORTES
    for (i = 0; i < qi; i++) {

        printf("\nEscolha o corte %d:\n", i + 1);
        printf("1 - CORTE DE CABELO - R$ 25,00\n");
        printf("2 - CORTE DA BARBA - R$ 15,00\n");
        printf("3 - CORTE GERAL - R$ 35,00\n");
        scanf("%d", &cortes[i]);

        if (cortes[i] == 1){
            valorT += 25;
        }
        else if (cortes[i] == 2){
            valorT += 15;
        }
        else if (cortes[i] == 3){
            valorT += 35;
        }
        else{
            printf("Corte invalido!\n");
            i--; 
        }
    }

    // DADOS
    printf("\nQual o seu nome? ");
    scanf("%s", nome);

    printf("Quantos anos voce tem? ");
    scanf("%f", &idade);

    valorSemDesconto = valorT;

    // PAGAMENTO
    printf("\nForma de pagamento:\n");
    printf("1 - Pix (5%% de desconto)\n");
    printf("2 - Dinheiro (5%% de desconto)\n");
    printf("3 - Cartao (sem desconto)\n");
    scanf("%d", &pagamento);

    if (pagamento == 1 || pagamento == 2){
        valorT *= 0.95;
    }

    printf("\nValor final: R$ %.2f\n", valorT);

    printf("Qual valor voce ira pagar? ");
    scanf("%f", &valorP);

    if (valorP >= valorT){
        troco = valorP - valorT;
    } else {
        printf("Valor insuficiente!\n");
    }

    // RESUMO
    printf("\n------ RESUMO DO PEDIDO ------\n");
    printf("Nome: %s\n", nome);
    printf("Idade: %.0f\n", idade);

    printf("\nCortes escolhidos:\n");
    for (i = 0; i < qi; i++) {
        if (cortes[i] == 1){
            printf("Corte %d: Cabelo (R$25)\n", i + 1);
        }
        else if (cortes[i] == 2){
            printf("Corte %d: Barba (R$15)\n", i + 1);
        }
        else if (cortes[i] == 3){
            printf("Corte %d: Geral (R$35)\n", i + 1);
        }
    }

    printf("\nForma de pagamento: ");
    if (pagamento == 1) printf("Pix\n");
    else if (pagamento == 2) printf("Dinheiro\n");
    else printf("Cartao\n");

    printf("Valor sem desconto: R$ %.2f\n", valorSemDesconto);
    printf("Valor final: R$ %.2f\n", valorT);

    if (pagamento == 1 || pagamento == 2){
        printf("Desconto aplicado: 5%%\n");
    }

    if (valorP >= valorT){
        printf("Valor pago: R$ %.2f\n", valorP);
        printf("Troco: R$ %.2f\n", troco);
    }

    printf("\nObrigado pela preferencia!\n");

    return 0;
}
return 0;
}
*/
